number = float(input("Escreva um número: "))

def soma (num):
    sum = number
    for numOfNumbers in range (5, 1, -1):
        num = float(input("Escreva um número: "))
        sum += num
    return sum

print(soma(number))

import
